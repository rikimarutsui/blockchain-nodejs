<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create Wallet</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <form method="post" action="/wallet/create" id="form" class="validate form">
        <h2>Create a Wallet</h2>
        <div class="form-field">
          <input type="submit" value="Create a Wallet" />
        </div>
        <div class="crtid">
            <% if (address) { %>
                <p class="btn">Address: <%= address %></p>
            <% } %>
            <% if (privateKey) { %>
                <p class="btn">Private Key: <%= privateKey %></p>
            <% } %>
        </div>
    </form>
    <script>
    </script>
</body>
</html>